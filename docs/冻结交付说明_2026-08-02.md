# MindPulse 冻结交付说明

- 冻结日期：2026-08-02
- 版本：`mindpulse@0.1.0`
- 构建：2026-08-02 16:37（Asia/Shanghai）
- 测试端点：`http://127.0.0.1:5180`
- Node / npm：`v26.1.0` / `11.13.0`

## 交付内容

冻结目录：`output/freeze-2026-08-02/`

只读压缩包：`output/MindPulse-freeze-2026-08-02.zip`

目录包含：

- `src/`：当前 React 和规则源码。
- `tests/`、`tools/`：可复现的自动化验证和浏览器规则构建工具。
- `dist/`：2026-08-02 最后一次生产构建。
- `docs/`：竞品研究、执行计划、状态矩阵、Privacy Receipt、删除边界、答辩脚本和走查模板。
- `evidence/`：普通、空白、部分记录、高风险、Privacy Receipt、漂流瓶六张 `487x872` 证据截图。
- `regression-2026-08-02.txt`：最后一次完整回归日志。

## 最终回归

通过的工程/浏览器命令包括：

```text
npm.cmd run build:browser-engine
npm.cmd run build:rule-cases
npm.cmd run typecheck
npm.cmd run test:domain
npm.cmd run test:profile
npm.cmd run test:parity
npm.cmd run test:security
npm.cmd run test:policy
npm.cmd run test:react:matrix
npm.cmd run test:react:handoff
npm.cmd run test:react:shell
npm.cmd run test:react:ui
npm.cmd run test:react:security
npm.cmd run test:react:storage
npm.cmd run test:react:bottle
npm.cmd run test:react:decision
npm.cmd run test:react:visual
npm.cmd run build
npm.cmd run capture:react:evidence
```

前两次冻结回归尝试的失败也保留在日志中：一次是旧的 `5181` 服务已退出导致连接拒绝；一次是部分记录测试的渲染时序。最终通过记录使用健康的 `5180` 服务，并将测试改为自包含空白夹具和等待完整页面条件。

## 明确边界

- 这是本机 Web / 手机画布 Demo，不是生产级服务。
- 本机 IndexedDB / localStorage 不是生产级加密存储；不承诺跨设备同步、设备丢失恢复、云端删除或导出文件自动擦除。
- 漂流瓶是本机匿名互动 Demo，不是真实多人社区、私信或运营系统。
- 分数和参考指数描述当前记录与规则边界，不是健康分数、诊断、危机预测或疗效证明。
- 系统不自动发送、自动联系、自动报警或自动上报。

## 仍待真实执行的外部证据

工程与材料已冻结，但下面两项不能由代码或合成 Fixture 替代：

1. 至少 3 名内部成员或目标用户的真实理解度走查，使用 `docs/用户理解度走查执行记录模板_2026-08-02.md`。
2. 至少 1 名真实专业审核者的审核意见，使用 `docs/专业审核记录模板_2026-08-02.md`。

在这两项有真实填写、可追溯的记录之前，不应宣称“已完成真实用户研究”或“专业审核已通过”。

## 交付范围决议

用户于 2026-08-02 明确同意当前先跳过上述两项外部验证。因此，本冻结包可作为 **Demo 与答辩工程交付版本** 使用；外部验证状态为“延期”，不是“完成”。后续补验或按专业意见修订时，必须使用新的日期创建新的冻结目录和压缩包，不能改写本冻结包的历史状态。
